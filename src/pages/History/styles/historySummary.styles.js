import { colors, fonts } from './tokens';

export const delta = {
  display: 'flex', alignItems: 'center', gap: 8,
  fontSize: 12, color: colors.muted, fontFamily: fonts.mono,
};

export const deltaValue = { color: colors.positive };
